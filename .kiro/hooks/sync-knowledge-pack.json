{
  "version": "v1",
  "hooks": [
    {
      "name": "能力包同步提醒",
      "description": "当Agent完成一个需求任务后，自动提醒检查XuebinLV能力包是否需要同步更新（新指标/新表/规则变更等）",
      "trigger": "Stop",
      "action": {
        "type": "agent",
        "prompt": "检查本次对话是否涉及以下任一变更，如果是则提醒用户\"是否需要同步更新能力包\"：1) 新增了指标或需求 2) 修改了已有指标的公式/口径 3) 新建了DWS/ADS表 4) 修改了产品线分类规则或公共规则 5) 发现了新的源系统坑。如果本次对话只是问答/查询/不涉及变更，则不输出任何提醒。"
      },
      "enabled": true
    }
  ]
}
