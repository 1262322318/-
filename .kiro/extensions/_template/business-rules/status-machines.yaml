# {团队} 业务规则 — 状态机模板

rules:
  - id: "{规则标识}"
    name: "{状态机名称}"
    entity: "{实体名（如：工单）}"
    field: "{状态字段名}"
    states:
      - value: "{状态值}"
        meaning: "{含义}"
        统计归类: "{统计时归为哪类}"
    transitions:
      - "{状态A} → {状态B}（{触发条件}）"
    统计注意:
      - "{统计指标时对该状态的处理}"
