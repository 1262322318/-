# WS-13 变更记录

## [1.0.0] 2026-07-31 - 初始版本

### 新增
- M1 应市项目平均开发周期：完整 ETL 逻辑落地
- 目标表 `dws_plm_project_dev_cycle_monthly` DDL
- 数据质量校验脚本

### 待落地
- M2 完成率：blocked（遗留-1 目标值模拟表未闭环）
- M3 同比改善：待确认历史数据策略（Q2）

### 已知限制
- ODS 物理列名未经 MCP 校验（MCP 不可用），采用 PLM 属性名小写假设
- `HBMTPDERIVETYPE` 字段域枚举值未校验，PA/PB 使用 LIKE 前缀匹配
- 小数位默认 1 位（遗留-2，待发起人确认）
- 暂停无恢复截断口径采用默认假设"统计月末"（Q1，待确认）
